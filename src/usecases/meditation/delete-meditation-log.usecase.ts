export class DeleteMeditationLogUsecase { constructor(private readonly persistence: import('../../domain/meditation/ports/meditation-persistence.port').MeditationPersistencePort) {} async execute(userId: string, id: string) { const existing = await this.persistence.findById(id); if (!existing || existing.userId !== userId) throw new Error('Record not found'); await this.persistence.delete(id); return { deleted: true }; } }
