import { DecodedIdToken } from 'firebase-admin/auth';
import { UserRole } from '../../common-utils/enums/user-role.enum';
import type { UserModel } from '../../domain/users/model/user.model';
import { UserPreferencesPersistencePort } from '../../domain/users/ports/user-preferences-persistence.port';
import { UsersPersistencePort } from '../../domain/users/ports/users-persistence.port';
export class SyncFirebaseUserUsecase { constructor(private readonly users: UsersPersistencePort, private readonly preferences: UserPreferencesPersistencePort, private readonly defaultRole: UserRole, private readonly superadminEmail?: string) {} async execute(firebaseUser: DecodedIdToken): Promise<UserModel> { const email = firebaseUser.email?.toLowerCase(); const role = email && email === this.superadminEmail?.toLowerCase() ? UserRole.SUPERADMIN : this.defaultRole; const payload = { firebaseUid: firebaseUser.uid, email, fullName: firebaseUser.name, avatarUrl: firebaseUser.picture, phone: firebaseUser.phone_number, provider: firebaseUser.firebase?.sign_in_provider, lastLoginAt: new Date() }; const existing = await this.users.findByFirebaseUid(firebaseUser.uid); const user = existing ? await this.users.update(existing.id, { ...payload, role: role === UserRole.SUPERADMIN ? UserRole.SUPERADMIN : existing.role }) : await this.users.create({ ...payload, role, language: 'fr', isActive: true }); if (!(await this.preferences.findByUserId(user.id))) await this.preferences.create({ userId: user.id, language: user.language ?? 'fr' }); return user; } }
