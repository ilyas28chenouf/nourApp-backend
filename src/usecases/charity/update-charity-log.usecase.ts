export class UpdateCharityLogUsecase { constructor(private readonly persistence: import('../../domain/charity/ports/charity-persistence.port').CharityPersistencePort) {} async execute(userId: string, id: string, data: any) { const existing = await this.persistence.findById(id); if (!existing || existing.userId !== userId) throw new Error('Record not found'); return this.persistence.update(id, data); } }
